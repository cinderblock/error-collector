printWidth: 120
singleQuote: true
arrowParens: avoid
trailingComma: all
# Accept the working tree's native line endings (CRLF on Windows via
# core.autocrlf) instead of forcing LF; git still stores LF in the blob.
endOfLine: auto
